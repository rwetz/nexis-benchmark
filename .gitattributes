# Normalize line endings to LF in the repo and on checkout (stops CRLF churn).
* text=auto eol=lf

# Binary assets — never touch.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.icns binary
*.woff binary
*.woff2 binary
*.gguf binary
*.onnx binary
